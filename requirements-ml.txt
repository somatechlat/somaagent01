########## Optional ML/Document-processing dependencies ##########
# Installed only when INCLUDE_ML_DEPS=true during Docker build.
# CPU-first: torch pinned to CPU wheels; GPU builds are opt-in via Docker build args.

# Core torch stack (CPU-only, no CUDA shards) — pinned by constraints-ml.txt
torch==2.3.1

# LangChain integrations for Unstructured
langchain-unstructured==0.1.6

# Embeddings, vector search, tokenization (torch CPU)
# DEPRECATED: FAISS is legacy (Agent Zero compat only). Use SomaBrain for memory.
sentence-transformers==2.4.0
# faiss-cpu==1.11.0


# Speech-to-text and audio processing
faster-whisper==1.0.3
openai-whisper==20240930; python_version < "3.13"
soundfile==0.13.1

# OCR and document parsing
pytesseract==0.3.13
pdf2image==1.17.0
pymupdf==1.25.3
pypdf==6.0.0

# Full Unstructured stack (very heavy)
unstructured[all-docs]==0.16.23
