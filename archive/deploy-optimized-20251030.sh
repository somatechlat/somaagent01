#!/bin/bash

# ARCHIVED (NEUTRALIZED): deploy-optimized script content removed during legacy purge.
# Use the canonical `docker-compose.yaml` and `Makefile` targets for development
# and deployment guidance in `docs/roadmap/canonical-roadmap.md`.

echo "This archived file has been neutralized. See docs/roadmap/canonical-roadmap.md for deployment guidance."
