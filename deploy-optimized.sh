#!/bin/bash

# DELETED: deploy-optimized.sh was removed as part of legacy cleanup.
# Use `docker-compose.yaml` and the provided Makefile targets (e.g., `make dev-up`)
# or adjust your deployment scripts to reference the canonical compose manifest.

echo "This script has been removed. See docs/roadmap/canonical-roadmap.md for deployment guidance."