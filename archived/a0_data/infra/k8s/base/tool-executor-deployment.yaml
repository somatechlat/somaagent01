apiVersion: apps/v1
kind: Deployment
metadata:
  name: tool-executor
  labels:
    app.kubernetes.io/name: tool-executor
    app.kubernetes.io/part-of: somaagent01
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: tool-executor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tool-executor
        app.kubernetes.io/part-of: somaagent01
    spec:
      containers:
        - name: tool-executor
          image: agent0ai/agent-zero:latest
          imagePullPolicy: IfNotPresent
          command: ["python"]
          args: ["-m", "services.tool_executor.main"]
          envFrom:
            - configMapRef:
                name: somaagent-config
            - secretRef:
                name: somaagent-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
