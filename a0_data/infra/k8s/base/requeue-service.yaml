apiVersion: v1
kind: Service
metadata:
  name: requeue-service
  labels:
    app.kubernetes.io/name: requeue-service
    app.kubernetes.io/part-of: somaagent01
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: requeue-service
  ports:
    - name: http
      port: 8012
      targetPort: http
