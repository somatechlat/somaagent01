default:
  fail_open: true
  budgets:
    default: 0  # 0 means unlimited
  routing:
    allow: []
    deny: []

enterprise:
  fail_open: false
  budgets:
    default: 500000
    qa_bot: 750000
  routing:
    allow:
      - meta-llama/Meta-Llama-3.1-8B-Instruct
    deny:
      - experimental-expensive-model
