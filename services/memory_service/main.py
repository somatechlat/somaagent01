"""Deprecated: gRPC MemoryService has been removed.

This module is kept only as a tombstone and will raise on import.
"""

raise ImportError(
    "services.memory_service has been removed; use SomaBrain HTTP via SomaClient"
)
