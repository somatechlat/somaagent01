flowchart TD
    subgraph Ingestion
        A[Source System] -->|Kafka Producer| B[Kafka Topic: raw-events]
    end
    subgraph Processing
        B -->|Kafka Consumer| C[Propagation Service]
        C --> D[Enrichment Module]
        D --> E[Kafka Topic: enriched-events]
    end
    subgraph Storage
        E -->|Consumer| F[PostgreSQL DB]
    end
    subgraph Monitoring
        C --> G[Prometheus Exporter]
        G --> H[Grafana Dashboard]
    end
