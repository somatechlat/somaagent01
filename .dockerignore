# Reduce Docker build context and keep secrets out of images

# VCS and editor noise
.git
.github
.gitmodules
.DS_Store
.vscode
*.swp

# Local virtualenvs and caches
.venv
__pycache__
*.py[cod]
*.pyo
*.pyd

# Test suites, artifacts, and golden assets (not needed at runtime)
tests/
webui/tests/
webui/tests/ui/node_modules/
webui/tests/ui/test-results/
webui/tests/ui/tmp/
webui/tests/ui/tokens/

# Temporary and local data
tmp/
logs/
kafka-logs/
postgres-backups/

# Documentation (exclude from runtime image)
docs/

# Node modules anywhere (defensive)
**/node_modules/

# Secrets and keys — compose injects via env_file
.env
*.pem

# Misc large or unused directories
memory/*
!memory/.gitkeep


# === VIBE CLEANUP - Added 2024-12-23 ===
# Deleted/legacy directories
bin/
src/core/infrastructure/db/
src/core/messaging/

# Build artifacts
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# IDE and project files
.kiro/
.gemini/
*.md
!README.md
!CHANGELOG.md

# Large ML models (should be mounted, not baked)
*.bin
*.onnx
*.pt
*.pth

# Alembic (deprecated - use Django migrations)
alembic/
alembic.ini

# UI dist files (served by CDN, not needed in backend image)
ui/frontend/dist/
ui/frontend/node_modules/

# Source maps
*.map

# Entire UI folder (frontend served separately)
ui/
