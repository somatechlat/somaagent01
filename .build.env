# Central build configuration
# Override these in CI or locally as needed
IMAGE_REPO=somaagent01
INCLUDE_ML_DEPS=false

# Optional registry (prefix IMAGE_REPO accordingly if you use a registry)
# REGISTRY=docker.io
