# Todo List

- **Refactor `services/common/session_repository.py`** – finalize usage of `cfg` for Redis URL and DSN (already done).
- **Verify PostgresSessionStore constructor** – ensure metrics and logging are correct.
- **Run `python -m compileall`** to confirm no syntax errors.
- **Commit changes to branch `kiro-branch`** and open PR to `master`.
