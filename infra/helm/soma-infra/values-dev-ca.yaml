# Developer CA values for soma-infra (optional)
# Enables cert-manager with a CA ClusterIssuer (dev-ca) that uses a local CA secret.

namespace: soma-infra

certManager:
  enabled: true
  type: ca
  clusterIssuer:
    name: dev-ca
  ca:
    secretName: dev-ca
