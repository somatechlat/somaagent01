apiVersion: v1
kind: Service
metadata:
  name: {{ include "delegation.fullname" . }}
  labels:
    app: {{ include "delegation.name" . }}
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "delegation.name" . }}
    release: {{ .Release.Name }}
