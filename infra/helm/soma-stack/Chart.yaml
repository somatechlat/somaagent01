apiVersion: v2
name: soma-stack
description: Helm chart for deploying the SomaAgent01 application stack (UI, gateway, memory service, conversation worker) on Kubernetes.
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: soma-infra
    version: ">=0.1.0"
    repository: "file://../soma-infra"