# S5-A – mTLS patch applied via kustomize
apiVersion: v1
kind: Pod
metadata:
  annotations:
    sidecar.istio.io/inject: "true"
spec:
  containers:
  - name: gateway
    env:
    - name: ISTIO_META_MESH_ID
      value: cluster.local
    - name: ISTIO_META_INTERCEPTION_MODE
      value: REDIRECT